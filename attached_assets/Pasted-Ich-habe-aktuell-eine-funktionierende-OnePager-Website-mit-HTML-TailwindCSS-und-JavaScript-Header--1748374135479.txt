Ich habe aktuell eine funktionierende OnePager-Website mit HTML, TailwindCSS und JavaScript (Header, Hero, Lösungen, Projekte, Kontakt). Jetzt möchte ich sie zu einer professionellen MultiPage-Seite erweitern. Bitte setze Folgendes für mich um:

🔁 Ziel: Startseite bleibt OnePager, aber zusätzlich:
1. Eine eigene Seite für **/projekte**
   - Zeige alle bisherigen Projekte in einem Masonry- oder Grid-Layout.
   - Optional: Filterfunktion nach Kategorien (z. B. „Duschkabinen“, „Vordächer“, „Brüstungen“).
   - Verlinke jedes Projekt zu einer Detailseite mit mehr Bildern/Text.

2. Für jede Hauptleistung eine eigene Unterseite:
   - `/duschkabinen`
   - `/glasvordaecher`
   - `/bruestungen`
   - Inhaltlich ähnlich wie auf der Startseite, aber ausführlicher.
   - Jeweils mit SEO-tauglicher H1, Textblöcken, Bildern, CTA.

3. Eine eigene **/kontaktformular**-Seite:
   - Enthält ein funktionierendes Kontaktformular mit `name`, `email`, `nachricht`.
   - Responsives Layout, hübsch und DSGVO-konform.
   - Nach dem Absenden → schicke zu einer `danke.html` oder zeige einen Inline-Erfolgshinweis.
   - Nutze vorhandenes JavaScript für Button + Validierung.

4. Navigation erweitern:
   - Navigation überarbeiten (Header & Footer), damit alle Seiten verlinkt sind.
   - Beispiel: Startseite, Lösungen (Dropdown), Projekte, Kontakt

🔧 Technische Vorgaben:
- Verwende TailwindCSS wie bisher.
- HTML-Dateien sollen klar benannt sein (z. B. `duschkabinen.html`, `kontaktformular.html`, `projekte.html`).
- Verwende die existierende `style.css` und `script.js`, aber erweiter sie bei Bedarf sinnvoll.
- Achte auf SEO-Elemente (title, meta description, H1…).

Optional:
- Baue einen Sticky CTA-Button „Beratung anfordern“ auf Mobilgeräten ein.
- Baue eine einfache Projekt-Detailseite (z. B. `projekt-luna.html`) als Vorlage.

Bitte gib mir:
✅ alle neuen HTML-Dateien  
✅ die angepasste Navigation  
✅ die empfohlenen Strukturänderungen in CSS/JS (wenn nötig)

Nutze meinen bestehenden Code als Grundlage. Fang an.